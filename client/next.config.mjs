/** @type {import('next').NextConfig} */
const nextConfig = {
  // Emits .next/standalone with a self-contained server.js, so the runtime image
  // does not need node_modules or the Next CLI.
  output: "standalone",
};

export default nextConfig;
